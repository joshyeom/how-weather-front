export interface AgeType{
    [key:string]: boolean
    age10: boolean,
    age2030: boolean,
    age40: boolean,
}

export interface GenderType{
    [key: string]: Boolean,
    male: boolean,
    female: boolean,
}

export interface BodyType{
    [key: string]: Boolean,
    hot: boolean 
    normal: boolean 
    cold: boolean
}